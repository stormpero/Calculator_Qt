#pragma once
#include "Allfunc.h"
#include "natural.h"

////////////////////////// Старосельский Александр  ///////////////////////////////////

vector<int> ABS_Z_N(vector<int> a);  // Абсолютная величина числа, результат - натуральное

//////////////////////////////////////////////////////////////////////////////

////////////////////////// Ермольев Максим ///////////////////////////////////

vector<int> MUL_ZZ_Z(vector<int> a, vector<int> b); // Умножение целых чисел

vector<int> MOD_ZZ_Z(vector<int> a, vector<int> b); // Остаток от деления большего целого числа на меньшее или равное натуральное с остатком(делитель отличен от нуля)

//////////////////////////////////////////////////////////////////////////////

////////////////////////// Астахов Михаил  ///////////////////////////////////

int POZ_Z_D(vector<int> num); // Определение положительности числа (2 - положительное, 0 — равное нулю, 1 - отрицательное)

vector<int> ADD_ZZ_Z(vector<int> num1, vector<int> num2); // Сложение целых чисел

//////////////////////////////////////////////////////////////////////////////

////////////////////////// Игорь Темников  ///////////////////////////////////

vector<int> MUL_ND_N(vector<int> entry, int number);  //Умножение целого на цифру

vector<int> SUB_ZZ_Z(vector<int> vector_1, vector<int> vector_2); // Вычитание целых чисел

vector<int> DIV_ZZ_Z(vector<int> vector_1_N, vector<int> vector_2_N);// Частное от деления большего целого числа на меньшее или равное натуральное с остатком

//////////////////////////////////////////////////////////////////////////////

////////////////////////// Табояков Иван  ///////////////////////////////////

vector<int> MUL_ZM_Z(vector<int> input); //Умножение целого числа на -1

//////////////////////////////////////////////////////////////////////////////

////////////////////////// Жиренкин Артем  ///////////////////////////////////

vector<int> TRANS_Z_N(vector<int> a) ;//Преобразование целого неотрицательного в натуральное

//////////////////////////////////////////////////////////////////////////////
